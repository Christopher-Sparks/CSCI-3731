1. Libraries are a convenient way to reuse and distribute code.

2. There are static libraries, which are linked into your executable file. And there is shared object libraries, which are separate from your executable and loaded at runtime. 